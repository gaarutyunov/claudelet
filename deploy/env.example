# Claudelet Environment Configuration
# Copy this file to /etc/claudelet/env and configure

# Session secret (generate with: openssl rand -base64 32)
SESSION_SECRET=change-me-to-a-secure-random-string

# Google OAuth (optional, for Google login)
# GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-client-secret
# GOOGLE_CALLBACK_URL=https://your-domain.com/api/auth/google/callback

# Email whitelist (comma-separated, leave empty to allow all)
ALLOWED_EMAILS=your-email@example.com

# CORS origins (comma-separated)
CORS_ORIGINS=https://your-domain.com

# Max sessions per user
MAX_SESSIONS_PER_USER=10

# Server settings (optional)
# PORT=3001
# HOST=0.0.0.0

# Data paths are relative to --workdir (default: current directory)
# Override with absolute paths if needed:
# DB_PATH=/custom/path/claudelet.db
# WORKSPACE_BASE_PATH=/custom/path/workspaces
# CLAUDE_CONFIG_PATH=/custom/path/claude-config
